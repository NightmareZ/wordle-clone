import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'makarov.wordle',
  appName: 'Вордли',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
